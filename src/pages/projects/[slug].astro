---
// src/pages/projects/[slug].astro
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import type { Project } from '../../data/projects';
import { projects } from '../../data/projects';

// 1) Genera todas las rutas en build
export async function getStaticPaths() {
	return projects.map((p) => ({
		params: { slug: p.slug },
		props: { project: p }, // se inyecta como Astro.props
	}));
}

// 2) Recibe el proyecto ya resuelto desde getStaticPaths
const { project } = Astro.props as { project: Project };
---

<Base
	title={`${project.title} · Nia Portfolio`}
	description={project.description ?? ''}>
	<Header />

	<main class='mx-auto max-w-6xl px-4 py-12'>
		<!-- Navegación -->
		<nav class="mb-8">
			<a
				href='/'
				class='inline-flex items-center gap-2 px-4 py-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full hover:bg-white/20 transition-colors text-sm font-medium'
			>
				<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
				</svg>
				Volver a inicio
			</a>
		</nav>

		<!-- Header del proyecto -->
		<div class="mb-12">
			{project.featured && (
				<div class="flex items-center gap-2 mb-4">
					<svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
						<path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
					</svg>
					<span class="text-yellow-400 font-medium">Proyecto destacado</span>
				</div>
			)}

			<h1 class='text-4xl md:text-6xl font-bold mb-6'>
				<span class="bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
					{project.title}
				</span>
			</h1>
			
			{project.description && (
				<p class='text-xl text-gray-300 max-w-3xl leading-relaxed mb-8'>
					{project.description}
				</p>
			)}

			<!-- Enlaces del proyecto -->
			<div class='flex flex-wrap gap-4 mb-8'>
				{project.url && (
					<a
						class='inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full font-semibold hover:scale-105 transition-transform shadow-lg'
						href={project.url}
						target='_blank'
					>
						<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
							<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
						</svg>
						Ver demo en vivo
					</a>
				)}
				{project.github && (
					<a
						class='inline-flex items-center gap-2 px-6 py-3 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full hover:bg-white/20 transition-colors font-semibold'
						href={project.github}
						target='_blank'
					>
						<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
							<path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
						</svg>
						Ver código fuente
					</a>
				)}
			</div>

			<!-- Tags -->
			{project.tags && (
				<div class='flex flex-wrap gap-2'>
					{project.tags.map((tag) => (
						<span class='px-4 py-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full text-sm font-medium'>
							{tag}
						</span>
					))}
				</div>
			)}
		</div>

		<!-- Imagen del proyecto -->
		{project.image && (
			<div class="mb-16">
				<div class="bg-gradient-to-r from-blue-500/10 to-purple-500/10 backdrop-blur-sm border border-white/20 rounded-3xl p-4">
					<img
						src={project.image}
						alt={project.title}
						class='w-full rounded-2xl shadow-2xl'
					/>
				</div>
			</div>
		)}

		<!-- Contenido detallado del proyecto -->
		<div class="grid lg:grid-cols-3 gap-12">
			<!-- Descripción principal -->
			<div class="lg:col-span-2 space-y-8">
				<div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-3xl p-8">
					<h2 class="text-2xl font-bold mb-6">
						<span class="bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
							Sobre este proyecto
						</span>
					</h2>
					<div class="prose prose-invert max-w-none">
						<p class="text-gray-300 leading-relaxed mb-4">
							Este proyecto demuestra mi capacidad para crear aplicaciones web modernas y funcionales. 
							Implementé las mejores prácticas de desarrollo y utilicé tecnologías de vanguardia para 
							ofrecer una experiencia de usuario excepcional.
						</p>
						<p class="text-gray-300 leading-relaxed mb-4">
							Durante el desarrollo, me enfoqué en la optimización del rendimiento, la accesibilidad 
							y la escalabilidad del código. El resultado es una aplicación robusta y bien estructurada.
						</p>
						<p class="text-gray-300 leading-relaxed">
							Cada decisión técnica fue cuidadosamente considerada para asegurar que el proyecto 
							cumpliera con los más altos estándares de calidad.
						</p>
					</div>
				</div>

				<!-- Características -->
				<div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-3xl p-8">
					<h3 class="text-xl font-bold mb-6">Características principales</h3>
					<div class="grid gap-4">
						<div class="flex items-start gap-3">
							<div class="w-6 h-6 bg-gradient-to-r from-blue-400 to-purple-400 rounded-full flex items-center justify-center mt-0.5">
								<svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
								</svg>
							</div>
							<div>
								<h4 class="font-semibold">Diseño responsivo</h4>
								<p class="text-sm text-gray-400">Optimizado para todos los dispositivos</p>
							</div>
						</div>
						<div class="flex items-start gap-3">
							<div class="w-6 h-6 bg-gradient-to-r from-blue-400 to-purple-400 rounded-full flex items-center justify-center mt-0.5">
								<svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
								</svg>
							</div>
							<div>
								<h4 class="font-semibold">Performance optimizado</h4>
								<p class="text-sm text-gray-400">Tiempos de carga rápidos</p>
							</div>
						</div>
						<div class="flex items-start gap-3">
							<div class="w-6 h-6 bg-gradient-to-r from-blue-400 to-purple-400 rounded-full flex items-center justify-center mt-0.5">
								<svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
								</svg>
							</div>
							<div>
								<h4 class="font-semibold">Código limpio</h4>
								<p class="text-sm text-gray-400">Siguiendo las mejores prácticas</p>
							</div>
						</div>
					</div>
				</div>
			</div>

			<!-- Información lateral -->
			<div class="space-y-6">
				<!-- Información técnica -->
				<div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-3xl p-6">
					<h3 class="font-semibold mb-4">Información técnica</h3>
					<div class="space-y-3 text-sm">
						<div>
							<span class="text-gray-400">Tipo:</span>
							<span class="ml-2">Aplicación web</span>
						</div>
						<div>
							<span class="text-gray-400">Estado:</span>
							<span class="ml-2 inline-flex items-center gap-1">
								<div class="w-2 h-2 bg-green-400 rounded-full"></div>
								Completado
							</span>
						</div>
						<div>
							<span class="text-gray-400">Año:</span>
							<span class="ml-2">2024</span>
						</div>
					</div>
				</div>

				<!-- Navegación a otros proyectos -->
				<div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-3xl p-6">
					<h3 class="font-semibold mb-4">Otros proyectos</h3>
					<div class="space-y-3">
						{projects.filter(p => p.slug !== project.slug).slice(0, 2).map((otherProject) => (
							<a href={`/projects/${otherProject.slug}`} class="block p-3 bg-white/10 rounded-xl hover:bg-white/20 transition-colors">
								<h4 class="font-medium text-sm">{otherProject.title}</h4>
								<p class="text-xs text-gray-400 mt-1 line-clamp-1">{otherProject.description}</p>
							</a>
						))}
						<a href="/#projects" class="block text-center py-2 text-sm text-blue-300 hover:text-blue-200 transition-colors">
							Ver todos los proyectos →
						</a>
					</div>
				</div>
			</div>
		</div>
	</main>
</Base>
